import React, {Component} from 'react';

export default class Specification extends Component {
    render() {
        return (
            <div>
                Hello work
            </div>
        );
    }
}
